# LaTeX build artifacts
*.aux
*.log
*.out
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz
*.bbl
*.blg

# Typst build cache
*.typstcache/

# Editor / OS backups & temp files
*~
*.swp
.goutputstream-*
.DS_Store

# Editor preferences
.vscode/

# PDF produits a partir des sources typst (regeneres par le hook pre-commit).
# Les autres PDF (datasheet, sujets de DM, corriges scannes, documents
# fournis...) restent suivis par git.
cours.pdf
TD.pdf
poly.pdf
TP.pdf
évaluation.pdf

# PDF derives des precedents par un outil externe (pypdf pour l'imposition du
# poly, 2-up pour la fiche d'evaluation) : artefacts eux aussi, mais le hook ne
# sait pas les regenerer -- le script n'est pas dans le depot.
poly-imprimable.pdf
évaluation x2.pdf
