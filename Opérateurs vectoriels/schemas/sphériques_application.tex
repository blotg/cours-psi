\begin{tikzpicture}[
    x= {(-0.353cm,-0.353cm)}, z={(0cm,1cm)}, y={(1cm,0cm)},
    scale=3,
    >=latex]
    \draw[->] (0,0,0) -- (1,0,0) node[below left] {$\vv{u_x}$};
    \draw[->] (0,0,0) -- (0,1,0) node[below right] {$\vv{u_y}$};
    \draw[->] (0,0,0) -- (0,0,1) node[above left] {$\vv{u_z}$};

    \tikzmath{
      \r=1.1;
      \t=60;
      \p=40;
      }
    \begin{scope}[canvas is plane={O(0,0,0)x({cos(\t)},{sin(\t)},0)y(0,0,1)}]
      \draw[dashed] (0,0) -- (\p:\r) coordinate (A) node[above right] {$M_1$};
      \draw[dashed] (A) -- ({\r*cos(\p)},0) -- (0,0);
    \end{scope}
    \fill (A) circle (0.7pt);

    \tikzmath{
      \r=1.1;
      \t=60;
      \p=0;
      }
    \begin{scope}[canvas is plane={O(0,0,0)x({cos(\t)},{sin(\t)},0)y(0,0,1)}]
      \draw[dashed] (0,0) -- (\p:\r) coordinate (A) node[below right] {$M_2$};
    \end{scope}
    \fill (A) circle (0.7pt);

    \tikzmath{
      \r=1.1;
      \t=-40;
      \p=40;
      }
    \begin{scope}[canvas is plane={O(0,0,0)x({cos(\t)},{sin(\t)},0)y(0,0,1)}]
      \draw[dashed] (0,0) -- (\p:\r) coordinate (A) node[above right] {$M_3$};
      \draw[dashed] (A) -- ({\r*cos(\p)},0) -- (0,0);
    \end{scope}
    \fill (A) circle (0.7pt);

    \tikzmath{
      \r=0.7;
      \t=60;
      \p=-20;
      }
    \begin{scope}[canvas is plane={O(0,0,0)x({cos(\t)},{sin(\t)},0)y(0,0,1)}]
      \draw[dashed] (0,0) -- (\p:\r) coordinate (A) node[below right] {$M_4$};
      \draw[dashed] (A) -- ({\r*cos(\p)},0) -- (0,0);
    \end{scope}
    \fill (A) circle (0.7pt);
  \end{tikzpicture}